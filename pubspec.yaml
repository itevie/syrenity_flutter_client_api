name: syrenity_flutter_client_api
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.2

# Add regular dependencies here.
dependencies:
  file_picker: ^11.0.2
  flutter:
    sdk: flutter
  http: ^1.6.0
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
